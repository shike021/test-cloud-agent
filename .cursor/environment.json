{
  "name": "test-cloud-agent",
  "install": "npm ci",
  "terminals": [
    {
      "name": "api",
      "command": "npm run dev:server",
      "description": "Express + SQLite REST API with hot reload on http://localhost:4000 (health: /api/health)."
    },
    {
      "name": "web",
      "command": "npm run dev:web",
      "description": "React + Vite dev server on http://localhost:5173, proxies /api to the API terminal."
    }
  ],
  "ports": [
    { "name": "api", "port": 4000 },
    { "name": "web", "port": 5173 }
  ]
}
